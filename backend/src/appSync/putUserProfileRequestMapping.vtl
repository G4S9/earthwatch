#set( $attributes = $util.dynamodb.toMapValues($context.arguments) )
{
  "version": "2017-02-28",
  "operation": "PutItem",
  "key": {
    "id": { "S": "${context.identity.cognitoIdentityId}" }
  },
  "attributeValues": $util.toJson($attributes)
}
